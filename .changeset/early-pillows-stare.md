---
"@guardian/google-admanager-api": patch
---

Add pnpm overrides to force versions between particular ranges for the following dependencies: `brace-expansion`, `esbuild`, `form-data`, `@babel/core`
